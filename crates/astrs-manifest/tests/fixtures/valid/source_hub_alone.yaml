# A node sourced from the AstRS package index, pinned to a revision — the
# short `name@rev` string form (blueprint §22's hub/package index).
nodes:
  - id: detector
    hub: yolo-detector@v0.3.1
    outputs: [detections]
